name: Build Linux

on:
  push:
    branches: [ "dev", "feature/actions" ]
  pull_request:
    branches: [ "dev" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'
        
    - name: Setup Python 3
      uses: actions/setup-python@v6
      with:
        python-version: '3.x'

    - name: Setup CMake
      uses: jwlawson/actions-setup-cmake@v2
      with:
        cmake-version: 'latest'
    
    - name: Build
      shell: bash
      run: ./jb/tools/mac/build.sh all x86_64

    - name: 'Upload Artifact'
      uses: actions/upload-artifact@v4
      with:
        name: jcef_mac_x64.tar.gz
        path: jcef_mac_x64.tar.gz
